{
  "name": "vendamais",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:api": "turbo dev --filter=@vendamais/api",
    "dev:web": "turbo dev --filter=@vendamais/web",
    "build:api": "turbo build --filter=@vendamais/api",
    "build:web": "turbo build --filter=@vendamais/web"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
